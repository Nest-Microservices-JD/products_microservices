# products_microservices
Microservices Products repository practice


## Development setup

1. Clone the repository
2. Install dependencies with `npm install`
3. Create a .env file with env.example file as a template
4. Execute `npx prisma migrate dev` to create the database and run migrations
5. Execute `npx prisma generate` to generate prisma client
6. Run `npm run start:dev`